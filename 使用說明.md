# 地震活動視覺化工具使用說明

## 概述

本工具提供地震活動（seismicity）統計與視覺化功能，可生成多種地震相關的圖表報告。

### 主要功能

1. **地震位置視覺化**
   - 深度分布圖
   - 剖面視圖
   - 地圖視圖
   - 叢集剖面圖

2. **地震規模分析**
   - 頻率-規模分布圖（FMD, Frequency-Magnitude Distribution）
   - 規模-時間序列圖

3. **多目錄比較分析**

4. **b值空間分布圖**

## 環境需求

### 必要套件

- Python 3.x
- NumPy
- Matplotlib
- ObsPy

### 安裝指令

```bash
pip install numpy matplotlib obspy
```

## 資料格式

### 地震目錄檔案格式

輸入的地震目錄檔案應為 CSV 格式，每行包含以下資訊（以逗號分隔）：

```
發震時間,緯度,經度,深度,規模
```

範例：
```
2019-07-04T17:33:49.000000Z,35.705,-117.504,8.0,6.4
2019-07-06T03:19:53.040000Z,35.770,-117.599,8.0,7.1
```

### 斷層資料格式

斷層資料檔案應使用 GMT 格式，以 `>` 開頭區分不同斷層段。

## 配置檔案說明

所有繪圖參數都在 `config.py` 中設定。根據您的需求選擇對應的配置類別：

### 1. Config_Loc - 地震位置繪圖配置

```python
class Config_Loc(object):
    def __init__(self):
        # 目錄資訊
        self.ctlg_path = 'catalog/your_catalog.csv'  # 地震目錄路徑
        self.fault_path = '/path/to/faults.dat'      # 斷層資料路徑
        self.lon_rng = [-118.0, -117.0]              # 經度範圍
        self.lat_rng = [35.0, 36.0]                  # 緯度範圍
        self.dep_rng = [0, 20.]                      # 深度範圍（公里）
        self.plot_prof = False                       # 是否繪製剖面圖
        
        # 繪圖參數
        self.fig_title = 'Your Title'                # 圖表標題
        self.fig_fname = 'output/your_plot.pdf'      # 輸出檔案名稱
        self.mark_size = 3.                          # 標記大小
        self.alpha = 0.6                             # 透明度
        self.cmap = 'hot'                            # 色階方案
```

### 2. Config_Mag - 規模分析配置

```python
class Config_Mag(object):
    def __init__(self):
        # 目錄資訊
        self.ctlg_path = 'catalog/your_catalog.csv'  # 地震目錄路徑
        self.mag_rng = [-1, 8.]                      # 規模範圍
        
        # 繪圖參數
        self.fig_title_fmd = 'FMD Title'             # FMD 圖標題
        self.fig_title_mt = 'M-t Title'              # M-t 圖標題
        self.mark_size = 10.                         # 標記大小
        self.alpha_fmd = 0.4                         # FMD 透明度
        self.alpha_mt = 0.6                          # M-t 透明度
```

### 3. Config_Cmp - 比較分析配置

```python
class Config_Cmp(object):
    def __init__(self):
        # 目錄資訊
        self.ctlg_list = ['catalog1.csv', 'catalog2.csv']  # 目錄清單
        self.name_list = ['Dataset1', 'Dataset2']           # 資料集名稱
        self.color_list = ['tab:blue', 'tab:orange']        # 顏色清單
        self.mag_rng = [-1, 8.]                             # 規模範圍
```

### 4. Config_b_Map - b值空間分布配置

```python
class Config_b_Map(object):
    def __init__(self):
        # 目錄資訊
        self.ctlg_path = 'catalog/your_catalog.csv'  # 地震目錄路徑
        self.lon_rng = [102.0, 104.0]                # 經度範圍
        self.lat_rng = [24.0, 26.0]                  # 緯度範圍
        
        # 計算參數
        self.xy_grid = 0.1                           # 網格大小（度）
        self.slice_radius = 0.15                     # 切片半徑（度）
        self.min_num = 200                           # 最小地震數量
        self.num_proc = 1                            # 處理器數量
        
        # 繪圖參數
        self.fig_fname = 'output/b_map.pdf'          # 輸出檔案名稱
        self.fig_title = 'b-value Map'               # 圖表標題
        self.b_rng = [0.7, 2.3]                      # b值範圍
        self.mc_rng = [0.3, 1.1]                     # Mc範圍
```

## 使用方式

### 1. 繪製地震位置圖

此功能可繪製地震在空間中的分布，包括地圖視圖和剖面視圖。

**步驟：**

1. 在 `config.py` 中編輯 `Config_Loc` 類別，設定您的參數
2. 確認地震目錄檔案路徑正確
3. 執行繪圖腳本：

```bash
python plot_loc.py
```

**輸出：**
- 地圖視圖 PDF 檔案（顯示地震震央分布）
- 剖面視圖 PDF 檔案（如啟用，顯示沿特定剖面的深度分布）

**圖表說明：**
- 顏色代表深度（預設使用 'hot' 色階，紅色較淺，黑色較深）
- 標記大小可反映地震規模
- 黑色線條為斷層位置

### 2. 繪製規模分析圖

此功能可分析地震規模的統計特性。

**步驟：**

1. 在 `config.py` 中編輯 `Config_Mag` 類別
2. 執行繪圖腳本：

```bash
python plot_mag.py
```

**輸出：**

將生成兩個圖表：

1. **頻率-規模分布圖（FMD）**
   - 顯示不同規模地震的發生頻率
   - 包含累積數量和非累積數量
   - 標註完整性規模（Mc）和 b 值
   - 虛線為 Gutenberg-Richter 定律擬合線

2. **規模-時間序列圖（M-t）**
   - 顯示地震規模隨時間的變化
   - 用於識別主震、餘震序列等特徵

**解讀說明：**
- **Mc（完整性規模）**：目錄完整記錄的最小規模
- **b值**：描述大小地震相對頻率的參數，通常約為 1.0
- b值偏高表示小地震相對較多，b值偏低表示大地震比例較高

### 3. 繪製多目錄比較圖

此功能可比較不同地震目錄的統計特性。

**步驟：**

1. 在 `config.py` 中編輯 `Config_Cmp` 類別
2. 設定要比較的目錄清單和對應名稱
3. 執行繪圖腳本：

```bash
python plot_cmp.py
```

**輸出：**
- 比較 FMD 圖（疊加多個目錄的頻率-規模分布）
- 比較 M-t 圖（疊加多個目錄的規模-時間序列）

**應用情境：**
- 比較不同定位方法的結果
- 比較不同研究團隊的地震目錄
- 評估目錄的完整性和一致性

### 4. 繪製 b 值空間分布圖

此功能計算並繪製 b 值的空間變化。

**步驟：**

1. 在 `config.py` 中編輯 `Config_b_Map` 類別
2. 設定計算參數（網格大小、半徑、最小地震數等）
3. 執行繪圖腳本：

```bash
python plot_b_map.py
```

**輸出：**
- b 值空間分布圖
- 完整性規模（Mc）空間分布圖
- 圖上疊加地震位置和斷層

**重要參數說明：**
- `xy_grid`：計算網格的大小，越小越精細但計算時間越長
- `slice_radius`：計算每個網格點時使用的地震半徑範圍
- `min_num`：計算 b 值所需的最小地震數量，太少會不可靠
- `num_proc`：使用的處理器數量，可設為大於 1 以加速計算

**注意事項：**
- b 值計算需要足夠的地震樣本，建議至少 200 個
- 計算時間取決於網格密度和資料量
- 可根據需要調整色階範圍以突顯空間變化

### 5. 繪製叢集剖面圖

此功能可繪製多個剖面的地震分布。

**步驟：**

1. 執行腳本（已將多個剖面參數寫在腳本中）：

```bash
python plot_cluster_profile.py
```

**輸出：**
- 多個剖面的地震深度分布圖
- 可用於研究複雜斷層系統的三維幾何

### 6. 繪製單一剖面圖

此功能可繪製指定剖面的地震分布。

**步驟：**

1. 在腳本中設定剖面端點和寬度
2. 執行腳本：

```bash
python plot_one_profile.py
```

**輸出：**
- 單一剖面的詳細地震分布圖
- 顯示沿剖面的距離與深度關係

## 輸出檔案

所有輸出圖表預設儲存為 PDF 格式，位於 `output/` 目錄下（需自行建立此目錄）。

建議的目錄結構：
```
Seismicity-Visualization/
├── catalog/          # 存放地震目錄檔案
├── output/           # 存放輸出圖表
├── config.py         # 配置檔案
├── plot_*.py         # 繪圖腳本
└── reader.py         # 資料讀取模組
```

## 常見問題

### Q1: 執行腳本時出現「找不到檔案」錯誤

**A:** 請確認：
- 地震目錄檔案路徑是否正確
- 是否已建立 `output/` 目錄
- 斷層資料路徑是否存在（或註解相關程式碼）

### Q2: 圖表顯示空白或沒有資料

**A:** 請檢查：
- 經緯度範圍是否涵蓋您的地震資料
- 深度和規模範圍是否合理
- 地震目錄檔案格式是否正確

### Q3: b 值計算結果不合理

**A:** 可能原因：
- 地震數量不足（建議至少 200 個）
- 規模範圍設定不當
- 目錄不完整，存在明顯的規模缺失

### Q4: 如何調整圖表的外觀？

**A:** 在 `config.py` 中可調整：
- `mark_size`：標記大小
- `alpha`：透明度（0-1）
- `cmap`：色階方案（如 'hot', 'cool', 'rainbow', 'viridis' 等）
- `fsize_label`、`fsize_title`：字體大小
- `fig_size`：圖表尺寸

### Q5: 可以輸出其他格式嗎？

**A:** 可以。在 `config.py` 中修改 `fig_fname` 的副檔名：
- `.pdf` - PDF 向量圖（推薦，適合發表）
- `.png` - PNG 點陣圖（適合網頁）
- `.jpg` - JPEG 圖片
- `.svg` - SVG 向量圖

## 進階使用

### 批次處理

可建立 shell 腳本進行批次處理。參考 `plot_sta.sh` 範例：

```bash
#!/bin/bash
# 批次處理多個時段的資料

for period in fore main post; do
    # 修改配置
    sed -i "s/catalog_name/${period}/g" config.py
    
    # 執行繪圖
    python plot_loc.py
    python plot_mag.py
    
    # 還原配置
    sed -i "s/${period}/catalog_name/g" config.py
done
```

### 自訂分析

您可以使用 `reader.py` 和 `statis_lib.py` 中的函式進行自訂分析：

```python
from reader import read_ctlg, slice_ctlg
from statis_lib import calc_mc_b, calc_fmd

# 讀取目錄
events = read_ctlg('catalog/your_catalog.csv')

# 篩選資料
events = slice_ctlg(events, 
                    lat_rng=[35.0, 36.0],
                    lon_rng=[-118.0, -117.0],
                    dep_rng=[0, 15],
                    mag_rng=[2.0, 8.0])

# 計算統計量
mag = events['mag']
mc, [b_val, b_dev] = calc_mc_b(mag)
print(f'Mc = {mc}, b = {b_val} ± {b_dev}')
```

## 參考資料

### 相關文獻

- Gutenberg, B., & Richter, C. F. (1944). Frequency of earthquakes in California. 
  Bulletin of the Seismological Society of America, 34(4), 185-188.

- Wiemer, S., & Wyss, M. (2000). Minimum magnitude of completeness in earthquake catalogs: 
  Examples from Alaska, the western United States, and Japan. 
  Bulletin of the Seismological Society of America, 90(4), 859-869.

### 套件文件

- ObsPy 文件：https://docs.obspy.org/
- Matplotlib 文件：https://matplotlib.org/
- NumPy 文件：https://numpy.org/doc/

## 技術支援

如有問題或建議，請透過 GitHub Issues 回報：
https://github.com/oceanicdayi/Seismicity-Visualization/issues

---

**最後更新日期：** 2026-02-09

**版本：** 1.0
